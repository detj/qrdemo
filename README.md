# QR Code Encoding Demo

Just playing with the [qr](https://npmjs.org/package/qr) npm module

## Run
Install the dependencies and run

```
npm install
node qrdemo.js
```

QR codes will be generated in the `/codes` directory
